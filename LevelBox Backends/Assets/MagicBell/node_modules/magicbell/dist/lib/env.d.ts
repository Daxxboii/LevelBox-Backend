/// <reference types="node" />
import { ClientOptions } from '../types';
export declare function getEnvInfo(): {
    binding: string;
    binding_version: string;
    publisher: string;
} | {
    runtime: string;
    runtime_version: string;
    platform: NodeJS.Platform;
    arch: NodeJS.Architecture;
    binding: string;
    binding_version: string;
    publisher: string;
};
export declare function getUserAgent(appInfo?: ClientOptions['appInfo']): string;
export declare function getClientUserAgent(appInfo?: ClientOptions['appInfo']): string;
